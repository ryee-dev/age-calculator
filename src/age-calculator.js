export class AgeCalculator {
  constructor(ageInput) {
    this.age = ageInput;
  }

  getsAge() {
    let mercYears = 0;
    let venusYears = 0;
    let earthYears = 0;
    let marsYears = 0;
    let jupiterYears = 0;
    if ( this.age === this.age ) {
      earthYears = this.age * 1;
      return earthYears;
    }
  }
}
